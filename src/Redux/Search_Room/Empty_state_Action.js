import axios from "axios";
import { createAsyncThunk } from "@reduxjs/toolkit";
import { url } from "../BaseURL";

export const Empty_state = createAsyncThunk('main_api/Empty_state', async (data) => {

    return response.data

})